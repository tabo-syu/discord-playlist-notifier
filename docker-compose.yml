version: '3'

services:
  bot:
    build: .
    tty: true
    volumes:
      - .:/go/src/bot
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: "postgres:14.4-alpine"
    volumes:
      - "db-data:/var/lib/postgresql/data"

volumes:
  db-data:
